generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://postgres:8159b010648307fa447c@dpbdp1.easypanel.host:321/korax?sslmode=disable"
}

model Lead {
  id               Int      @id @default(autoincrement())
  name             String
  email            String
  whatsapp         String
  audienceSize     String
  currentRevenue   String
  createdAt        DateTime @default(now())
} 